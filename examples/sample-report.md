# csp-doctor report

**75/100 (C)** across **3** policy(ies) — 2 error(s), 8 warning(s), 2 info(s)

## examples/weak.csp.txt — 57/100 (F)

| | Category | Directive | Issue | Fix |
| - | -------- | --------- | ----- | --- |
| ✗ | XSS exposure | `script-src` | 'unsafe-inline' in script-src defeats XSS protection | Remove 'unsafe-inline' and use a per-request nonce (`'nonce-…'`) or hashes, plus `'strict-dynamic'`. |
| ⚠ | XSS exposure | `script-src` | 'unsafe-eval' allows eval() / new Function() | Remove 'unsafe-eval' and refactor any eval()/new Function() usage. |
| ⚠ | XSS exposure | `script-src` | `https:` scheme source in script-src | List specific hosts instead of the bare `https:` scheme. |
| ⚠ | Allowlist bypass | `script-src` | Allowlisted host enables a CSP bypass: ajax.googleapis.com | Drop the host and switch to a nonce + 'strict-dynamic' (which ignores host allowlists). |
| ⚠ | Missing directives | `base-uri` | No base-uri | Add `base-uri 'none'` (or `'self'`). It does not fall back to default-src. |
| ⚠ | Missing directives | `frame-ancestors` | No frame-ancestors | Add `frame-ancestors 'self'` (or `'none'`). |
| ℹ | Missing directives | `object-src` | object-src is not 'none' | Add `object-src 'none'` unless you genuinely embed plugins. |

## examples/strong.csp.txt — 100/100 (A)

✓ No issues found.

## examples/page.html (meta) — 69/100 (D)

| | Category | Directive | Issue | Fix |
| - | -------- | --------- | ----- | --- |
| ✗ | XSS exposure | `script-src` | 'unsafe-inline' in script-src defeats XSS protection | Remove 'unsafe-inline' and use a per-request nonce (`'nonce-…'`) or hashes, plus `'strict-dynamic'`. |
| ⚠ | Allowlist bypass | `script-src` | Allowlisted host enables a CSP bypass: cdn.jsdelivr.net | Drop the host and switch to a nonce + 'strict-dynamic' (which ignores host allowlists). |
| ⚠ | Missing directives | `base-uri` | No base-uri | Add `base-uri 'none'` (or `'self'`). It does not fall back to default-src. |
| ⚠ | Missing directives | `frame-ancestors` | No frame-ancestors | Add `frame-ancestors 'self'` (or `'none'`). |
| ℹ | Missing directives | `object-src` | object-src is not 'none' | Add `object-src 'none'` unless you genuinely embed plugins. |

---
<sub>Generated by csp-doctor v0.1.0 · 2026-06-05T01:32:03.138Z</sub>
